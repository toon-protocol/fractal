{
  "name": "@toon-protocol/feed-portal",
  "version": "0.0.0",
  "private": true,
  "description": "Feed portal skeleton: the browser app that renders a fractal dimension by npub — dittos visually distinct from interpretation, provenance on demand, profile/seed/spec viewable. Free to browse; no wallet or engagement writes (toon-meta#245, fractal#13).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@toon-protocol/fractal": "workspace:*",
    "nostr-tools": "^2.24.1"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
